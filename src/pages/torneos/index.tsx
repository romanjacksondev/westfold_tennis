import TournamentsView from 'views/Tournaments';
import MainLayout from '../../layout';

const Torneos = () => <MainLayout component={TournamentsView} pageTitle={`Torneos`} />;

export default Torneos;
