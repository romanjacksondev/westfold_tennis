/** @format */

import MainLayout from "app/Layout";
import ExcusatorView from "views/ExcusatorView";

const Excusator = () => (
  <MainLayout component={ExcusatorView} pageTitle={`Excusador`} />
);
export default Excusator;
